server:
  port: "8088"

spring:
  application:
    name: "provider"

eureka:
  instance:
    appname: "provider"
  client:
    service-url:
      defaultZone: "http://localhost:8761/eureka/"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: "always"

from: "111from"

